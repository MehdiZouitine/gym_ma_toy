from setuptools import setup

setup(
    name="gym_ma_toy",
    version="0.0.1",
    packages=["gym_ma_toy"],
    install_requires=["gymnasium>=1.0.0", "numpy", "matplotlib", "PILLOW", "aenum"],
    # And any other
    # dependencies required
)
